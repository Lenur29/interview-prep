# =========== E2E Test Targets ============
# URLs the Playwright tests hit. Defaults match the shared local dev stack.
# Copy this file to .env.yml and adjust per-workspace hostnames if needed.

API_URL: https://api.lemurjs.local
APP_URL: https://app.lemurjs.local
