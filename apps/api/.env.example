# Copy this file to .env and fill in real values for your environment.
# Production values come from your deploy platform (Fly.io secrets, Supabase, etc.).

# PostgreSQL connection string. For local dev, this matches docker-compose.yml.
DATABASE_URL="postgresql://lemur:lemur_dev@localhost:5432/lemur?schema=public"
