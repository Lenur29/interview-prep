mutation loginWithTelegramWebapp($input: LoginWithTelegramWebappInput!) {
  loginWithTelegramWebapp(input: $input) {
    token
    user {
      id
      email
      firstName
      lastName
    }
  }
}
